name: CLI Tool check and build
on:
  push:
    path:
      - 'cli/**'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - name: Test
        run: |
          cd cli
          make test
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.19
      - name: Test
        run: |
          cd cli
          make build
      - uses: actions/upload-artifact@v2
        with:
          name: code-review-tool-cli-${{ github.run_id }}
          path: ${{ github.workspace }}/cli/out/code-review-tool-cli
          retention-days: 1
